"""Ported v2 render engine — providers, video assembly, audio/dub utils.

Single home for the generation engine (was the repo-root src/). The render
handler drives it directly; no sys.path tricks, no parallel v2/v3 split.
"""
