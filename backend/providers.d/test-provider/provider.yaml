id: test-provider
name: Test Provider
version: "1.0"
description: A dummy provider for integration tests
capabilities:
  - text_to_video
  - image_to_video
models:
  - id: test-model-v1
    family: test
    capabilities: [text_to_video]
    max_duration_s: 10
    max_resolution: [1080, 1920]
    cost:
      per_second_usd: 0.01
      per_request_usd: 0.05
    latency:
      p50_s: 5
      p95_s: 15
      timeout_s: 60
    strengths: [fast, cheap]
cost:
  per_second_usd: 0.01
  per_request_usd: 0.05
latency:
  p50_s: 5
  p95_s: 15
  timeout_s: 60
adapter:
  type: python
  entrypoint: adapter.py
tags:
  - test
  - local
